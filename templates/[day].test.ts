import {} from './[day]'

const testInput = `[testInput]`

describe('day[day]Part01', () => {
  it('should ', () => {
    expect(1).toEqual(1)
  })
})

describe('day[day]Part02', () => {
  it('should', () => {
    expect(1).toEqual(1)
  })
})
