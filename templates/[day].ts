import { readFile } from 'fs/promises'
export const partOne = async (): Promise<number> => {
  const input = await readFile('src/[year]/inputs/[day].txt', 'utf8')
  return 0
}

export const partTwo = async (): Promise<number> => {
  const input = await readFile('src/[year]/inputs/[day].txt', 'utf8')
  return 0
}
